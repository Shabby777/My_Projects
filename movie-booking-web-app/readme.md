# Silver Screen Booking

A React + Vite + TypeScript movie booking demo with a premium cinematic UI, static movie data, seat selection, mock checkout, booking confirmation, and saved tickets stored in local browser storage.

## Scripts

- `npm.cmd install`
- `npm.cmd run dev`
- `npm.cmd run build`
